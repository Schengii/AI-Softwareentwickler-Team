import asyncio

class SafeMetrics:
    def __init__(self):
        self._lock = asyncio.Lock()
        self.requests = 0

    async def inc_requests(self):
        async with self._lock:
            self.requests += 1
