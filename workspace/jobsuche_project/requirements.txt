# -------------------------------------------------
# Basis‑Abhängigkeiten für das gesamte Projekt
# -------------------------------------------------
fastapi==0.110.2
uvicorn[standard]==0.30.1

# Nur für das Parser‑Modul (Standard‑Library reicht, aber für mögliche Erweiterungen)
pydantic==2.7.1          # optional, falls später Validierung via Pydantic gewünscht wird

# Entwicklungs‑Werkzeuge
pytest==8.2.2
pytest-cov==5.0.0
